android_run_script_sudo
=======================

With this class you can run scripts like a shell in Android environment. ONLY WORKS WITH ROOT ANDROID



ScriptRunner runner = new ScriptRunner(String script, StringBuilder response);
runner.start();
